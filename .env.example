# ===================================================================
# 加密货币交易机器人 - Go 版本配置文件
# Crypto Trading Bot - Go Version Configuration File
# ===================================================================
# 将此文件复制为 .env 并填入你的实际配置
# Copy this file to .env and fill in your actual configuration
# 命令 / Command: cp .env.example .env
# ===================================================================

# ===================================================================
# 第一部分：项目路径配置（可选，使用默认值即可）
# Part 1: Project Paths Configuration (Optional, defaults are fine)
# ===================================================================

# 结果保存目录 / Results directory
# 说明 / Description: 保存交易日志、分析报告、状态记录
# 默认值 / Default: ./crypto_results
# RESULTS_DIR=./crypto_results

# 数据缓存目录 / Data cache directory
# 说明 / Description: 缓存市场数据，减少 API 调用
# 默认值 / Default: ./internal/dataflows/data_cache
# DATA_CACHE_DIR=./internal/dataflows/data_cache

# 数据库路径 / Database path
# 说明 / Description: SQLite 数据库文件路径，存储交易会话历史
# 默认值 / Default: ./data/trading.db
# DATABASE_PATH=./data/trading.db

# ===================================================================
# 第二部分：LLM（大语言模型）配置 ⚙️ 核心配置
# Part 2: LLM Configuration ⚙️ Core Settings
# ===================================================================

# LLM 提供商 / LLM Provider
# 可选值 / Options: openai, anthropic, google, ollama
# 说明 / Description:
#   - openai: 使用 GPT 模型（推荐，稳定性好）/ Use GPT models (recommended, stable)
#   - anthropic: 使用 Claude 模型（推理能力强）/ Use Claude models (strong reasoning)
#   - google: 使用 Gemini 模型 / Use Gemini models
#   - ollama: 使用本地模型（需自己部署）/ Use local models (self-hosted)
# 默认值 / Default: openai
LLM_PROVIDER=openai

# 深度思考模型 / Deep thinking model
# 用途 / Purpose: 用于最终交易决策，需要更强的推理能力
# OpenAI 推荐 / Recommended: gpt-4o, gpt-4-turbo, o1-preview
# Anthropic 推荐 / Recommended: claude-3-opus-20240229
# 说明 / Description: 使用更强大的模型，决策质量更高但成本也更高
# 默认值 / Default: gpt-4o
DEEP_THINK_LLM=gpt-4o

# 快速思考模型 / Quick thinking model
# 用途 / Purpose: 用于日常数据分析，响应速度要求快
# OpenAI 推荐 / Recommended: gpt-4o-mini, gpt-3.5-turbo
# Anthropic 推荐 / Recommended: claude-3-haiku-20240307
# 说明 / Description: 使用较小的模型降低成本，适合频繁调用
# 默认值 / Default: gpt-4o-mini
QUICK_THINK_LLM=gpt-4o-mini

# LLM API 后端地址 / LLM API backend URL
# 默认值 / Default: https://api.openai.com/v1（OpenAI 官方）
# 其他选项 / Other options:
#   - https://openrouter.ai/api/v1（OpenRouter，支持多种模型）
#   - 自建代理服务器地址 / Custom proxy server
# 说明 / Description: 如果使用非 OpenAI 官方 API，修改此项
LLM_BACKEND_URL=https://api.openai.com/v1

# OpenAI API 密钥 / OpenAI API Key ⚠️ 必需 / Required
# 用途 / Purpose: 调用 LLM 进行决策分析
# 获取 / Get from: https://platform.openai.com/api-keys
# 格式 / Format: sk-proj-xxxxxxxxxxxxxxxx
# 注意 / Note: 确保账户有足够余额 / Ensure sufficient balance
OPENAI_API_KEY=your-openai-api-key-here

# 交易策略 Prompt 文件路径 / Trading strategy prompt file path ⭐ 新功能 / New Feature
# 说明 / Description: 从外部文件加载交易策略 Prompt，方便测试不同策略
# 用途 / Purpose: 快速切换不同的交易哲学和决策标准
# 可用选项 / Available options:
#   - prompts/trader_system.txt: 趋势交易，极度选择性（推荐）/ Trend trading, highly selective (recommended)
#   - prompts/trader_aggressive.txt: 短线交易，积极捕捉机会 / Short-term trading, aggressive
#   - 或创建自己的 Prompt 文件 / Or create your own prompt file
# 好处 / Benefits:
#   - 无需重新编译即可修改交易策略 / Modify strategy without recompiling
#   - A/B 测试不同 Prompt 的效果 / A/B test different prompts
#   - 版本控制和回溯 / Version control and history
# 注意 / Note: 修改后需要重启机器人 / Restart bot after changes
# 默认值 / Default: prompts/trader_system.txt
# TRADER_PROMPT_PATH=prompts/trader_system.txt

# ===================================================================
# 第三部分：智能体行为配置
# Part 3: Agent Behavior Configuration
# ===================================================================

# 投资辩论轮数 / Debate rounds
# 说明 / Description: Bull 和 Bear 分析师的辩论次数（目前未使用）
# 范围 / Range: 1-5 轮
# 默认值 / Default: 2
MAX_DEBATE_ROUNDS=2

# 风险讨论轮数 / Risk discussion rounds
# 说明 / Description: 风险分析师的讨论次数（目前未使用）
# 范围 / Range: 1-5 轮
# 默认值 / Default: 2
MAX_RISK_DISCUSS_ROUNDS=2

# 最大递归限制 / Max recursion limit
# 说明 / Description: 防止无限递归的保护机制
# 范围 / Range: 1-1000
# 默认值 / Default: 100
MAX_RECUR_LIMIT=100

# ===================================================================
# 第四部分：数据源配置
# Part 4: Data Vendor Configuration
# ===================================================================

# 股票/加密货币价格数据供应商 / Stock/Crypto price data vendor
# 可选值 / Options: ccxt, yfinance, alpha_vantage
# 说明 / Description: ccxt 专为加密货币设计（推荐）/ ccxt is designed for crypto (recommended)
# 默认值 / Default: ccxt
DATA_VENDOR_STOCK=ccxt

# 技术指标数据供应商 / Technical indicators data vendor
# 可选值 / Options: ccxt, yfinance
# 说明 / Description: 基于价格数据计算技术指标 / Calculate indicators from price data
# 默认值 / Default: ccxt
DATA_VENDOR_INDICATORS=ccxt

# 新闻数据供应商 / News data vendor
# 可选值 / Options: alpha_vantage, openai, google
# 说明 / Description: 目前新闻分析未集成到 Go 版本
# 默认值 / Default: alpha_vantage
DATA_VENDOR_NEWS=alpha_vantage

# 加密货币专属数据供应商 / Crypto-specific data vendor
# 可选值 / Options: ccxt
# 说明 / Description: 提供资金费率、订单簿等加密货币特有数据
# 默认值 / Default: ccxt
DATA_VENDOR_CRYPTO=ccxt

# ===================================================================
# 第五部分：币安交易配置 ⚠️ 重要 / Important
# Part 5: Binance Trading Configuration
# ===================================================================

# 币安 API 密钥 / Binance API Key ⚠️ 实盘交易必需 / Required for live trading
# 用途 / Purpose: 连接币安交易所，获取数据和执行交易
# 获取 / Get from: https://www.binance.com/zh-CN/my/settings/api-management
# 权限 / Permissions: 启用"现货和杠杆交易"、"期货"，禁用"提现"
# 安全 / Security: 建议设置 IP 白名单，定期更换密钥
# 注意 / Note: 测试模式下可以留空或使用无效密钥
BINANCE_API_KEY=your-binance-api-key-here
BINANCE_API_SECRET=your-binance-api-secret-here

# 币安代理地址 / Binance Proxy (可选 / Optional)
# 说明 / Description: 如果无法直接访问币安，需要设置代理
# 格式 / Format: http://host:port 或 socks5://host:port
# 示例 / Example: http://192.168.0.226:6152
# 注意 / Note: 不需要代理时可以删除或留空 / Remove or leave empty if not needed
# BINANCE_PROXY=http://192.168.0.226:6152

# 代理 TLS 验证跳过开关 / Proxy TLS verification skip
# 可选值 / Options: true, false
# 说明 / Description: 某些代理（如 Clash、V2Ray）会修改 TLS 证书
# 用途 / Purpose: 当出现 TLS 证书验证错误时，设置为 true
# 安全警告 / Security Warning: 跳过 TLS 验证会降低安全性
# 默认值 / Default: false
# BINANCE_PROXY_INSECURE_SKIP_TLS=true

# 币安杠杆倍数 / Binance Leverage ⭐ 支持动态杠杆 / Supports Dynamic Leverage
# 格式 / Format:
#   - 固定杠杆 / Fixed leverage: 单个数字，如 "10"
#   - 动态杠杆 / Dynamic leverage: 范围格式，如 "10-20" ⭐ 新功能
#
# 固定杠杆示例 / Fixed leverage example:
#   BINANCE_LEVERAGE=10  # 所有交易都使用 10 倍杠杆
#
# 动态杠杆示例 / Dynamic leverage example:
#   BINANCE_LEVERAGE=10-20  # LLM 根据置信度在 10-20 倍之间选择
#
# 动态杠杆说明 / Dynamic leverage explanation:
#   - LLM 会根据置信度、趋势强度（ADX）、波动性（ATR）智能选择杠杆
#   - 置信度 0.75-0.80 → 使用范围下限（如 10 倍）
#   - 置信度 0.80-0.85 → 使用范围中值（如 15 倍）
#   - 置信度 0.85-0.90 → 使用范围中上值（如 17 倍）
#   - 置信度 0.90+ → 使用范围上限（如 20 倍）
#   - 在最确定的机会上使用更高杠杆，追求大赢
#   - 在不确定的机会上使用较低杠杆，控制风险
#
# 范围 / Range: 1-125 倍
# 风险等级 / Risk Level:
#   - 1-3倍: 低风险，适合新手 / Low risk, suitable for beginners
#   - 5-10倍: 中等风险（推荐）/ Medium risk (recommended)
#   - 10-20倍: 高风险 / High risk
#   - 20倍以上: 极高风险，容易爆仓 ⚠️ / Very high risk, easy to liquidate
#
# 建议配置 / Recommended configuration:
#   - 新手 / Beginner: BINANCE_LEVERAGE=5 (固定 5 倍)
#   - 进阶 / Intermediate: BINANCE_LEVERAGE=8-12 (动态 8-12 倍)
#   - 高级 / Advanced: BINANCE_LEVERAGE=10-20 (动态 10-20 倍)
#
# 注意 / Note: 杠杆越高，盈利和亏损都会被放大 / Higher leverage amplifies both gains and losses
# 默认值 / Default: 10
BINANCE_LEVERAGE=10

# 测试模式开关 / Test Mode ⚠️⚠️⚠️ 强烈建议 / Highly Recommended
# 可选值 / Options: true, false
# 说明 / Description:
#   - true: 仅模拟交易，不会实际下单（币安测试网）/ Simulation only (Binance Testnet)
#   - false: 实盘交易，会真实下单 ⚠️ 有资金风险！/ Live trading with real money
# 强烈建议 / Strongly Recommended: 在充分测试验证策略之前，始终保持为 true
# 默认值 / Default: true
BINANCE_TEST_MODE=true

# 持仓模式 / Position Mode
# 可选值 / Options: oneway, hedge, auto
# 说明 / Description:
#   - oneway: 单向持仓模式，只能持有多仓或空仓其中之一 / One-way mode, long or short only
#   - hedge: 双向持仓模式，可同时持有多仓和空仓 / Hedge mode, can hold both long and short
#   - auto: 自动检测（推荐）/ Auto-detect (recommended)
# 注意 / Note: 持仓模式需要在币安交易所设置，且一经设置较难更改
# 默认值 / Default: auto
BINANCE_POSITION_MODE=auto

# ===================================================================
# 第六部分：交易参数配置 📊 核心参数
# Part 6: Trading Parameters Configuration
# ===================================================================

# 交易对列表 / Trading Pairs ⭐ 支持单个或多个
# 格式 / Format: 逗号分隔（可以有空格）/ Comma-separated (spaces allowed)
# 示例 / Examples:
#   - 单个交易对 / Single pair: CRYPTO_SYMBOLS=BTC/USDT
#   - 多个交易对 / Multiple pairs: CRYPTO_SYMBOLS=BTC/USDT,ETH/USDT
#   - 带空格也可以 / With spaces: CRYPTO_SYMBOLS=BTC/USDT, ETH/USDT, BNB/USDT
# 常见选项 / Common options:
#   - BTC/USDT: 比特币 / Bitcoin
#   - ETH/USDT: 以太坊 / Ethereum
#   - BNB/USDT: 币安币 / Binance Coin
#   - SOL/USDT: Solana
# 说明 / Description:
#   - 交易对必须是币安期货支持的 / Must be supported by Binance Futures
#   - 系统会并行分析所有交易对 / System analyzes all pairs in parallel
#   - Trader 会综合所有分析，选择最优的交易机会 / Trader selects best opportunities
# 功能 / Features:
#   - 并行技术分析、链上数据分析、情绪分析
#   - 智能仓位分配，避免过度分散
#   - 风险管理和回撤控制
# 建议 / Recommendation: 不要超过 3 个交易对，避免过度分散资金
# 示例使用 / Example usage:
# CRYPTO_SYMBOLS=BTC/USDT,ETH/USDT

# K线时间周期 / Candlestick timeframe
# 可选值 / Options: 15m, 1h, 4h, 1d
# 说明 / Description:
#   - 15m: 超短线，高频交易（仅供经验丰富者）/ Ultra-short term, high frequency
#   - 1h: 短线，日内交易（推荐）/ Short term, day trading (recommended)
#   - 4h: 中短线 / Medium-short term
#   - 1d: 长线，波段交易 / Long term, swing trading
# 注意 / Note: 系统会根据此周期自动安排运行时间
# 建议 / Recommendation: 新手使用 1h 或 4h，避免过度交易
# 默认值 / Default: 1h
CRYPTO_TIMEFRAME=1h

# 数据回看天数 / Lookback days (可选 / Optional)
# 说明 / Description: 获取历史 K 线数据的天数，用于计算技术指标
# 智能推荐 / Smart recommendation (如果不设置，系统会自动根据时间周期选择):
#   - 15m → 5天（约 480 根 K 线）/ 5 days (~480 candles)
#   - 1h → 10天（约 240 根 K 线）/ 10 days (~240 candles)
#   - 4h → 15天（约 90 根 K 线）/ 15 days (~90 candles)
#   - 1d → 60天（约 60 根 K 线）/ 60 days (~60 candles)
# 影响 / Impact:
#   - 天数越多，数据越全面，但会增加 LLM context 负担
#   - 天数越少，分析速度快，但可能遗漏长期趋势
# 建议 / Recommendation: 留空使用智能推荐 / Leave empty to use smart defaults
# 格式 / Format: 正整数（天数）/ Positive integer (days)
# CRYPTO_LOOKBACK_DAYS=10

# ===================================================================
# ⚠️ 仓位管理说明 / Position Management Note
# ===================================================================
# POSITION_SIZE 和 MAX_POSITION_SIZE 已移除
# Removed: POSITION_SIZE and MAX_POSITION_SIZE
#
# 系统现在完全依赖 LLM 的仓位建议。LLM 必须在决策中提供仓位百分比，例如：
# The system now fully relies on LLM's position size recommendations. LLM must provide position percentage in decisions, e.g.:
#
#   **仓位建议**: 40%资金
#   Position recommendation: 40% of funds
#
# 如果 LLM 未提供仓位建议，系统将拒绝交易并报错。
# If LLM doesn't provide position recommendation, the system will reject the trade with an error.
#
# 请确保你的交易 Prompt（prompts/trader_system.txt）中包含仓位建议的要求。
# Ensure your trading prompt (prompts/trader_system.txt) requires position size recommendations.
# ===================================================================

# ===================================================================
# 第七部分：止损管理配置 🛡️ 核心风控功能
# Part 7: Stop-Loss Management Configuration 🛡️ Core Risk Control
# ===================================================================
#
# ⚠️ 重要说明 / Important Notes:
# 止损管理系统是资金保护的最后防线，遵循以下交易哲学：
# Stop-loss system is the last line of defense, following these principles:
#   1. 极度选择性 - 只交易高确定性机会 / Extreme selectivity
#   2. 高盈亏比 (>2:1) - 追求大赢 / High risk/reward ratio
#   3. 快速止损 - 错了就认 / Fast stop-loss
#   4. 让盈利奔跑 - 用追踪止损捕捉大行情 / Let profits run with trailing stops
#
# 详细文档 / Full documentation: docs/STOP_LOSS_GUIDE.md
#

# 止损策略类型 / Stop-loss strategy type
# 可选值 / Options: fixed, breakeven, trailing
# 说明 / Description:
#   - fixed: 仅固定止损，不自动调整（最保守）/ Fixed stop only, no adjustment
#   - breakeven: 固定止损 + 移动到成本价（保本）/ Fixed + move to breakeven
#   - trailing: 完整追踪止损系统（推荐）/ Full trailing stop system (recommended)
# 推荐 / Recommendation: trailing（符合"让盈利奔跑"哲学）
# 默认值 / Default: trailing
STOPLOSS_STRATEGY=trailing

# 是否启用保本机制 / Enable breakeven
# 可选值 / Options: true, false
# 说明 / Description: 当达到 1:1 盈亏比时，自动将止损移至成本价
# 好处 / Benefits: 锁定"不亏"状态，消除心理压力 / Lock in "no loss" state
# 触发条件 / Trigger: 见 STOPLOSS_BREAKEVEN_TRIGGER
# 推荐 / Recommendation: true（强烈推荐）/ Highly recommended
# 默认值 / Default: true
STOPLOSS_ENABLE_BREAKEVEN=true

# 保本触发点（盈亏比）/ Breakeven trigger ratio
# 范围 / Range: 0.01-0.10（1%-10%）
# 说明 / Description: 当盈利达到此百分比时，移动止损到成本价
# 示例 / Example: 0.025 = 2.5% 利润时保本 / Move to breakeven at 2.5% profit
# 盈亏比 / Risk:Reward: 0.025 ≈ 1:1（风险和收益相等）
# 建议 / Recommendation:
#   - 超短线 15m: 0.02 (2%)
#   - 短线 1h: 0.025 (2.5%) ← 推荐
#   - 中线 4h: 0.03 (3%)
# 默认值 / Default: 0.025 (2.5%, 1:1 risk/reward)
STOPLOSS_BREAKEVEN_TRIGGER=0.025

# 是否启用追踪止损 / Enable trailing stop
# 可选值 / Options: true, false
# 说明 / Description: 启用追踪止损，让止损价格随价格上涨自动上移
# 核心哲学 / Core philosophy: 让盈利奔跑，捕捉大行情 / Let profits run, catch big trends
# 数据支持 / Data support: 追踪止损收益是固定止盈的 3.3 倍！
# 推荐 / Recommendation: true（强烈推荐！）/ Highly recommended!
# 默认值 / Default: true
STOPLOSS_ENABLE_TRAILING=true

# 追踪止损触发点（盈亏比）/ Trailing stop trigger ratio
# 范围 / Range: 0.03-0.10（3%-10%）
# 说明 / Description: 当盈利达到此百分比时，启动追踪止损
# 示例 / Example: 0.05 = 5% 利润时启动追踪 / Start trailing at 5% profit
# 盈亏比 / Risk:Reward: 0.05 ≈ 2:1（符合高盈亏比哲学）
# 建议 / Recommendation:
#   - 超短线 15m: 0.03-0.04 (3-4%)
#   - 短线 1h: 0.05 (5%) ← 推荐
#   - 中线 4h: 0.06-0.08 (6-8%)
# 默认值 / Default: 0.05 (5%, 2:1 risk/reward)
STOPLOSS_TRAILING_TRIGGER=0.05

# 初始追踪距离 / Initial trailing distance
# 范围 / Range: 0.01-0.05（1%-5%）
# 说明 / Description: 追踪止损启动后，止损价格距离最高价的百分比
# 示例 / Example: 0.03 = 止损位在最高价下方 3% / Stop 3% below highest price
# 作用 / Purpose: 平衡利润保护和避免过早止损
# 建议 / Recommendation:
#   - 紧止损（易被震出）: 0.015-0.02 (1.5-2%)
#   - 平衡止损（推荐）: 0.03 (3%) ← 推荐
#   - 宽止损（捕捉大趋势）: 0.04-0.05 (4-5%)
# 杠杆影响 / Leverage impact: 高杠杆应使用更紧的止损
# 默认值 / Default: 0.03 (3%)
STOPLOSS_TRAILING_DISTANCE_INITIAL=0.03

# 收紧后的追踪距离 / Tightened trailing distance
# 范围 / Range: 0.01-0.03（1%-3%）
# 说明 / Description: 利润达到一定程度后，收紧追踪距离以保护利润
# 示例 / Example: 0.02 = 收紧到 2% / Tighten to 2%
# 触发条件 / Trigger: 见 STOPLOSS_TRAILING_TIGHTEN_PROFIT
# 目的 / Purpose: 在大幅盈利时，保护更多利润不被回吐
# 建议 / Recommendation:
#   - 紧收紧: 0.01-0.015 (1-1.5%)
#   - 平衡收紧: 0.02 (2%) ← 推荐
#   - 宽收紧: 0.025-0.03 (2.5-3%)
# 默认值 / Default: 0.02 (2%)
STOPLOSS_TRAILING_DISTANCE_TIGHT=0.02

# 收紧触发利润 / Profit to tighten trailing
# 范围 / Range: 0.08-0.20（8%-20%）
# 说明 / Description: 当盈利达到此百分比时，收紧追踪距离
# 示例 / Example: 0.10 = 利润达到 10% 时收紧 / Tighten at 10% profit
# 逻辑 / Logic: 已有丰厚利润，应更积极保护 / Protect profits more aggressively
# 建议 / Recommendation:
#   - 超短线 15m: 0.08 (8%)
#   - 短线 1h: 0.10 (10%) ← 推荐
#   - 中线 4h: 0.12-0.15 (12-15%)
# 默认值 / Default: 0.10 (10%)
STOPLOSS_TRAILING_TIGHTEN_PROFIT=0.10

# 是否启用分批止盈 / Enable partial take profit
# 可选值 / Options: true, false
# ⚠️ 不推荐 / Not Recommended: 违反"让盈利奔跑"哲学
# 说明 / Description: 在达到一定盈利时，平掉部分仓位落袋为安
# 缺点 / Drawbacks:
#   - 砍断盈利，错失大行情 / Cut profits short, miss big moves
#   - 降低总体收益 / Reduce overall returns
#   - 增加交易成本（手续费）/ Increase trading costs
# 适用场景 / Use cases: 仅在极高波动或心理压力大时使用
# 推荐 / Recommendation: false（保持仓位完整）
# 默认值 / Default: false
STOPLOSS_ENABLE_PARTIAL_TP=false

# 分批止盈比例 / Partial take profit ratio
# 范围 / Range: 0.1-0.5（10%-50%）
# 说明 / Description: 触发时平掉多少比例的仓位
# 示例 / Example: 0.3 = 平掉 30% 仓位 / Close 30% of position
# 建议 / Recommendation: 0.25-0.30（如果必须使用的话）/ 0.25-0.30 if you must use
# 默认值 / Default: 0.3 (30%)
STOPLOSS_PARTIAL_TP_RATIO=0.3

# 分批止盈触发点 / Partial take profit trigger
# 范围 / Range: 0.05-0.15（5%-15%）
# 说明 / Description: 当盈利达到此百分比时，执行分批止盈
# 示例 / Example: 0.075 = 7.5% 利润时平部分仓 / Close partial at 7.5% profit
# 盈亏比 / Risk:Reward: 0.075 ≈ 3:1
# 建议 / Recommendation: 如果启用，建议在 3:1 盈亏比以上 / If enabled, above 3:1
# 默认值 / Default: 0.075 (7.5%, 3:1 risk/reward)
STOPLOSS_PARTIAL_TP_TRIGGER=0.075

# ===================================================================
# 止损管理最佳实践 / Stop-Loss Best Practices
# ===================================================================
#
# 📊 推荐配置组合 / Recommended Configurations:
#
# 1. 【推荐】平衡型（适合1h交易）/ Balanced (for 1h trading):
#    STOPLOSS_STRATEGY=trailing
#    STOPLOSS_ENABLE_BREAKEVEN=true
#    STOPLOSS_BREAKEVEN_TRIGGER=0.025
#    STOPLOSS_ENABLE_TRAILING=true
#    STOPLOSS_TRAILING_TRIGGER=0.05
#    STOPLOSS_TRAILING_DISTANCE_INITIAL=0.03
#    STOPLOSS_TRAILING_DISTANCE_TIGHT=0.02
#    STOPLOSS_TRAILING_TIGHTEN_PROFIT=0.10
#    STOPLOSS_ENABLE_PARTIAL_TP=false
#
# 2. 保守型（适合新手）/ Conservative (for beginners):
#    - 使用更紧的追踪距离：0.02-0.025
#    - 更早收紧：0.08 盈利时收紧
#    - 可考虑启用分批止盈（但不推荐）
#
# 3. 激进型（捕捉大趋势）/ Aggressive (catch big trends):
#    - 使用更宽的追踪距离：0.04-0.05
#    - 更晚收紧：0.12-0.15 盈利时收紧
#    - 绝不使用分批止盈
#
# 📈 数据支持 / Data Support:
# 基于 100 次交易的回测数据（胜率 55%）:
#   - 固定止盈 (3%): 总收益 +82%
#   - 追踪止损 (3% 距离): 总收益 +270%（3.3倍！）
#   - 关键: 追踪止损能捕捉到 10-20% 的大行情
#
# ⚠️ 警告 / Warnings:
# 1. 追踪止损意味着需要忍受利润回吐
# 2. 不要频繁查看持仓，相信系统管理
# 3. 在测试模式下运行至少1周后再实盘
# 4. 查看文档了解更多：docs/STOP_LOSS_GUIDE.md
#

# ===================================================================
# 第八部分：记忆系统配置（实验性功能）
# Part 8: Memory System Configuration (Experimental)
# ===================================================================

# 是否启用记忆系统 / Enable memory system
# 可选值 / Options: true, false
# 说明 / Description:
#   - true: 系统会记住过往交易，从成功和失败中学习 / Learn from history
#   - false: 每次都是全新分析，不参考历史 / Fresh analysis each time
# 功能 / Features: 记录交易决策和结果，用于反思和优化
# 注意 / Note: 目前未在 Go 版本中完全实现 / Not fully implemented in Go version
# 默认值 / Default: true
USE_MEMORY=true

# 检索记忆数量 / Memory retrieval count
# 范围 / Range: 1-10
# 说明 / Description: 每次决策时，从记忆库中检索最相关的历史记录数量
# 影响 / Impact: 数量越多，参考越全面，但可能引入噪音 / More history = more context but possible noise
# 建议 / Recommendation: 3-5 条
# 默认值 / Default: 3
MEMORY_TOP_K=3

# ===================================================================
# 第九部分：调试和执行选项
# Part 9: Debug and Execution Options
# ===================================================================

# 调试模式 / Debug mode
# 可选值 / Options: true, false
# 说明 / Description:
#   - true: 显示详细日志和分析过程（推荐学习时使用）/ Detailed logs (recommended for learning)
#   - false: 只显示最终结果，输出简洁 / Minimal output
# 用途 / Purpose: 理解 AI 的决策逻辑，发现问题 / Understand AI logic, find issues
# 性能 / Performance: 不影响决策质量，只影响日志详细程度 / Only affects logging verbosity
# 默认值 / Default: false
DEBUG_MODE=false

# 选择的分析师 / Selected analysts
# 说明 / Description: 目前 Go 版本使用固定的分析师组合，此选项暂不生效
# 固定组合 / Fixed combination: market, crypto, sentiment, position
# 默认值 / Default: market,crypto,sentiment
SELECTED_ANALYSTS=market,crypto,sentiment

# 自动执行交易开关 / Auto-execution switch ⚠️⚠️⚠️ 危险 / Dangerous
# 可选值 / Options: true, false
# 说明 / Description:
#   - false: 仅生成交易建议，不实际下单（强烈推荐）/ Generate suggestions only
#   - true: 自动执行交易 ⚠️ 极度危险！/ Auto-execute trades - Very dangerous!
# 警告 / Warning: 除非你完全理解系统行为并充分测试，否则不要设置为 true
# 建议 / Recommendation: 永远保持为 false，人工审核后再手动交易 / Always keep false, manual review
# 安全提示 / Safety Tips:
#   - 即使启用自动执行，也要确保 BINANCE_TEST_MODE=true 先测试
#   - 监控账户余额，确保 LLM 提供合理的仓位建议
#   - 随时准备关闭系统或手动平仓
# 默认值 / Default: false
AUTO_EXECUTE=false

# ===================================================================
# 第十部分：Web 监控配置（可选）
# Part 10: Web Monitoring Configuration (Optional)
# ===================================================================

# Web 监控端口 / Web monitoring port
# 说明 / Description: Web 界面的端口号，用于监控交易状态
# 用途 / Purpose: 实时查看持仓、决策历史、性能指标
# 访问 / Access: http://localhost:8080
# 注意 / Note: 仅用于 Web 模式（make run-web）/ Only for web mode
# 默认值 / Default: 8080
WEB_PORT=8080

# ===================================================================
# 配置完成后的下一步 / Next Steps After Configuration:
# ===================================================================
# 1. 保存此文件为 .env / Save this file as .env
#    命令 / Command: cp .env.example .env
#
# 2. 填入你的真实 API 密钥 / Fill in your actual API keys
#    - OPENAI_API_KEY (必需 / Required)
#    - BINANCE_API_KEY 和 BINANCE_API_SECRET (实盘必需 / Required for live)
#
# 3. 根据你的风险偏好调整参数 / Adjust parameters based on your risk tolerance
#    - 新手建议 / Beginner recommendation:
#      BINANCE_TEST_MODE=true
#      AUTO_EXECUTE=false
#      BINANCE_LEVERAGE=5
#      MAX_DRAWDOWN=0.10
#
# 4. 构建和测试 / Build and test:
#    make build
#    make test
#
# 5. 运行单次分析（推荐先测试）/ Run single analysis (recommended first):
#    make run
#
# 6. 启动 Web 监控模式（持续运行）/ Start web monitoring mode (continuous):
#    make run-web
#
# 7. 查询历史数据 / Query historical data:
#    make query ARGS="stats"
#    make query ARGS="latest 10"
#
# ===================================================================
# 安全提示 / Safety Reminders:
# ===================================================================
# ⚠️ 永远先使用测试模式 / Always use test mode first
# ⚠️ 从小额资金开始 / Start with small amounts
# ⚠️ 监控回撤和风险 / Monitor drawdown and risks
# ⚠️ 定期备份数据库 / Regularly backup database
# ⚠️ 不要过度交易 / Don't overtrade
# ⚠️ 设置止损和止盈 / Set stop-loss and take-profit
# ===================================================================
